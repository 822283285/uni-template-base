import $store from './storage'
import $c, { parser, themeRegistry, getColor } from './style-utils/index'
import $nav from './nav'
import $constant from './constant'
import $data from './data'

export {
    $store,
    $c,
    $nav,
    $constant,
    $data,

    themeRegistry,
    getColor,
    parser
}
